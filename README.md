# DNP-Printer-Infos-CLI
Cross-platform CLI for querying DNP photo printers over USB, with Python and C# implementations. Outputs printer status, media, and remaining prints as text or JSON — no hot folder required.
